# WizardOS dependencies — updated for Python 3.12–3.14 (July 2026)
# Install with: pip install -r requirements.txt
# Or for dev: pip install -e ".[dev]"

# Qt6 GUI framework — 6.8.1+ supports Python 3.13; 6.10.1+ supports 3.14
# Keep the upper bound in the PySide6 major-version range. Do not use <3.15 here:
# PySide6 itself is versioned as 6.x, while Python compatibility is declared separately.
PySide6>=6.8.1,<6.15

# Computer vision — OpenCV 5.x works; pin to <6 for API stability
opencv-python>=4.10.0,<6

# Hand tracking — 0.10.35 ships py3-none wheels working on all Python 3.x
mediapipe>=0.10.35

# Numerical arrays — 2.x requires Python 3.12+
numpy>=2.0.0

# Audio synthesis — pygame-ce supports Python 3.14; original pygame lacks 3.14 wheels
pygame-ce>=2.5.6

# Rich terminal logging
rich>=13.9.0

# PyInstaller for packaging — optional, comment out if not building
pyinstaller>=6.11.0
